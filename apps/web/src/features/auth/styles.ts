import { tw } from "@/shared/ui/tw";

/** Shared field label vocabulary for `/login` and `/register`. */
export const authFieldLabel = `text-[0.65rem] font-semibold uppercase tracking-[0.16em] ${tw.muted}`;
